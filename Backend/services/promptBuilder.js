const buildPrompt = (subject, level) => {
  return `Create an engaging learning guide for "${subject}" at a "${level}" level. Include examples, best practices, and beginner-friendly explanations.`;
};

export default buildPrompt;
